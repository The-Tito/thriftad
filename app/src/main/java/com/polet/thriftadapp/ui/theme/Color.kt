package com.polet.thriftadapp.ui.theme

import androidx.compose.ui.graphics.Color

// El morado de tu logo, bordes y botón Entrar
val PurplePrimary = Color(0xFF6A1B9A)

// El fondo gris muy clarito que se ve detrás de la tarjeta blanca
val LightBG = Color(0xFFF5F6F9)

// El blanco puro de la tarjeta central (Card)
val White = Color(0xFFFFFFFF)

// El color de los placeholders y textos secundarios (estudiante, hogar...)
val TextGray = Color(0xFF9E9E9E)

// El color del borde cuando el campo no está seleccionado
val BorderGray = Color(0xFFE0E0E0)

val SecondaryPurple = Color(0xFF9575CD) // El morado claro para el degradado